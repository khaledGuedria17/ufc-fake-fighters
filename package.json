{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "Fake REST API using json-server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
